#pragma once

#include "Registry.hpp"

class PhysicsSystem
{
private:
	Registry* registry = NULL;

public:
	PhysicsSystem() {}
	PhysicsSystem(Registry* p_registry) : registry(p_registry) {}

	detect
};